numpy
fastapi
uvicorn
redis
opensearch-py
httpx  # Needed for async HTTP calls to Ollama
openai  # Required for OpenAI GPT-4 API calls
python-dotenv  # (Optional) To manage environment variables like OPENAI_API_KEY from a .env file

# Machine learning libraries for embeddings (if required elsewhere in the code)
torch>=1.13.1
transformers>=4.27.0
sentence-transformers>=2.2.2
